#:schema node_modules/wrangler/config-schema.json
name = "nextcalc-rate-limiter"
account_id = "95fec9f47d9285b4ad7a28e9262bd20e"
main = "src/index.ts"
compatibility_date = "2026-02-15"
compatibility_flags = ["nodejs_compat"]

# Observability and monitoring
[observability]
enabled = true

# KV namespace bindings for rate limit storage
[[kv_namespaces]]
binding = "RATE_LIMITS"
id = "65390e47b1ca4604bd58e5c4078e063b"
preview_id = "65390e47b1ca4604bd58e5c4078e063b" # Using production namespace for preview; create a separate preview namespace if needed

# Custom domain routing
[[routes]]
pattern = "ratelimit.nextcalc.io"
custom_domain = true

# CORS configuration and shared variables
[vars]
ALLOWED_ORIGINS = "http://localhost:3020,https://nextcalc.io,https://nextcalc.pro"
# ADMIN_KEY is set via `wrangler secret put ADMIN_KEY` (not in vars)
